global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Alerts appear in the Alertmanager UI (http://localhost:9093). Add webhook_configs
    # or email_configs here for paging in production.
